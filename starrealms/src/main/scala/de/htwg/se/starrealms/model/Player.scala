package de.htwg.se.starrealms.model

class Player() {




    /*var name: String = ""
    var authority: Int = 50
    var defaultDeck: Int = 10

    def this(name: String) = {
        this(name, 50, 10)
    }

    def this(name: String, authority: Int) = {
        this(name, authority, 10)
    }

    def this(name: String, authority: Int, defaultDeck: Int) = {
        this(name, authority, defaultDeck)
    }


    override def toString: String = s"Player($name, $authority, $defaultDeck)"
    def getName: String = name
    def getAuthority: Int = authority
    def getDefaultDeck: Int = defaultDeck*/

}